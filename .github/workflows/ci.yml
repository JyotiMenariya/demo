name: Test and Deploy

on:
  push:

env:
  APP_NAME: my-app

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy
        env:
          TOKEN: ${{ secrets.DEPLOY_TOKENS }}
        runs: |
          echo "Deploying with tokens length: ${#TOKENS}"
